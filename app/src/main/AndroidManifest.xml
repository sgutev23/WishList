<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hackzurich.wishlist" >

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme" >
        <meta-data android:name="com.facebook.sdk.ApplicationId" android:value="@string/app_id"/>

        <activity
            android:name=".MainActivity2"
            android:label="@string/title_activity_main">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".FriendWishlistActivity"
            android:label="@string/title_activity_friend_wishlist" >
        </activity>
        <activity android:name=".LoginActivity" android:label="Login with Facebook"/>
        <activity android:name="com.facebook.LoginActivity"/>
        <activity android:name=".TabbedActivity"/>
    </application>

</manifest>
